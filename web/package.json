{
  "name": "ncs-bank-web",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "NCS PASS 웹 — 순수 로직(src/core)은 브라우저 없이 시험한다",
  "scripts": {
    "test": "node --test test/*.test.js",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^8.0.0"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^6.1.0",
    "react": "^19.2.8",
    "react-dom": "^19.2.8"
  }
}
