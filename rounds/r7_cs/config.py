# -*- coding: utf-8 -*-
"""제7회 — 공기업 전산직 전공 (8과목 40문항 / 50분)

**앞의 여섯 회차와 다른 종류다.** r1~r6 은 NCS 직업기초능력이고, 이것은
전산 전공이다. 앱에서도 직렬로 갈라 보인다.

## 왜 전공만 내나

전산직 후기 153건(2025~2026)이 말하는 시험은 대체로 **두 교시**다 —

    한국부동산원   「1교시 NCS / 2교시 전공」   50문항 50분
    대구교통공사   NCS 40 + 전산학개론
    한국철도공사   NCS 30 + 컴퓨터일반 30 + 철도법령 10
    인천시설공단   전산학개론 단독 50문항 40분

NCS 교시는 **이미 r1~r6 여섯 벌이 있다.** 비어 있는 것은 전공 교시 쪽이라,
거기에 한 벌을 채운다. 전산직 응시자는 「NCS 회차 + 이 회차」로 하루를 맞춘다.
후기가 실제로 두 교시로 치르므로 이것은 타협이 아니라 그 모양 그대로다.

## 40문항 / 50분

후기의 문항 수는 50(30건)·40(18)·60(18), 시간은 60분(37건)·50분(26건)이
최빈이다. 다만 이 값들은 **NCS 를 포함한 것**이 섞여 있어 그대로 못 쓴다.
전공 단독으로 확인된 것은 인천시설공단 50문항 40분(48초)과 코레일 전공
30문항 계열이다. 그 사이에서 **40문항 50분 — 문항당 75초**로 잡았다.

전산 전공은 계산이 실제로 손을 탄다. 서브네팅 한 문항, 스케줄링 한 문항이
NCS 자료해석보다 오래 걸린다. r6(67.5초)보다 여유를 둔 이유다.

## 과목 배분 — 후기 언급 빈도로 가중했다

후기 153건에서 과목별 어휘가 걸린 건수를 세고, 최대잔여법으로 40을 나눴다.
바닥은 2 — 한 과목이 1문항만 나오면 그 과목은 회차에 있으나 마나다.

    네트워크 30건 → 8      운영체제      14건 → 4
    데이터베이스 28건 → 8   소프트웨어공학 13건 → 4
    프로그래밍언어 26건 → 7  전자계산기구조  8건 → 2
    정보보안 16건 → 5      데이터통신     2건 → 2

**첫 셈은 버렸다.** 「프로그래밍언어 61건」이 나왔는데 `코드`·`Java` 같은
넓은 낱말이 다른 과목의 후기까지 끌어온 것이었다. 과목을 가르는 어휘만
남겨 다시 세니 26건이다.

## 차례는 교재 차례를 따른다

하드웨어 → 시스템 소프트웨어 → 데이터 → 통신 → 공학 → 언어 → 보안.
2문항짜리 과목이 앞에 서지만 그것이 준비운동이 된다.

회차 블루프린트와 근거는 같은 폴더의 README.md.
"""
BRAND = "공기업 전산직 채용대비"
EXAM_TITLE = "전산직 봉투모의고사"
EXAM_ROUND = "전산 실전모의고사 제1회"
FILE_TAG = "전산_봉투모의고사_1회"   # 출력 PDF 파일명 접두사
TOTAL_Q = 40
TOTAL_MIN = 50

# 번호는 이 선언 순서로 매겨진다 (`build.py` 가 glob 이 아니라 이 표를 쓴다).
# 파일명은 은행의 `bank/_common/major_cs_*.py` 와 같은 꼬리를 쓴다 — 같은 과목을
# 두 군데서 다른 이름으로 부르지 않는다.
AREAS = [
    ("s1_ca",       "전자계산기구조", 2),   # 01~02
    ("s2_os",       "운영체제",      4),   # 03~06
    ("s3_database", "데이터베이스론", 8),   # 07~14
    ("s4_dc",       "데이터통신",    2),   # 15~16
    ("s5_network",  "네트워크",      8),   # 17~24
    ("s6_se",       "소프트웨어공학", 4),   # 25~28
    ("s7_pl",       "프로그래밍언어", 7),   # 29~35
    ("s8_security", "정보보안",      5),   # 36~40
]

# 회차 프로파일 — 플레이북의 고정 수치 대신 이 값을 목표로 삼는다 (D48).
PROFILE = {
    "문항당 시간": "75초 — 50분 / 40문항. r6(67.5초)보다 여유를 둔다. "
                   "서브네팅·스케줄링은 실제로 손이 간다",
    "검증 가능 비율": "**34문항(85%)을 코드로 확정한다.** 전산 전공은 정답이 "
                      "지문 밖 사실로 정해져, 틀리면 막을 수단이 계산밖에 없다. "
                      "은행 300건이 그 규율로 불일치 0 을 지켰다. "
                      "`rounds/r7_cs/verify.py` 가 sqlite·폐포·선행 그래프·시뮬레이션으로 다시 구한다",
    "실행·표준·개념": "실행 34 · 표준 4(20 캡슐화 · 21 DHCP · 27 UML · 34 자바 생성자) · "
                      "개념 2(38 전자봉투 · 40 웹취약점). 개념형은 과목당 1문항을 넘기지 않는다",
    "세트문항 비율": "0% — 전공은 한 문항이 한 사실이다. 같은 자료로 둘을 묶으면 "
                     "앞을 틀린 사람이 뒤도 잃는다. 전 문항 단독으로 낸다",
    "자료 길이": "코드·표·다이어그램 위주. 줄글 지문은 쓰지 않는다",
    # NCS 회차는 각주가 드물다(r6 는 3~5개). 각주 하나가 문제의 열쇠가 되는 장치라서다.
    # 전공은 다르다. 「비선점인가」 「오프셋을 8로 나누나」 「참조비트가 1이면 지우나」를
    # 적지 않으면 **문항이 여러 답을 갖는다.** 열쇠가 아니라 **전제**를 못 박는 쓰임이라
    # 문항마다 하나쯤 붙는다. 실측 42개 — 이것을 줄이는 것은 문항을 흐리는 일이다.
    "각주(※) 개수": "문항당 1개 안팎 — 단위·전제(비선점인지, 8의 배수인지)를 못 박는다. "
                    "열쇠가 아니라 전제다",
    "난이도": "실기 아닌 필기 수준. 계산은 2단계까지. "
              "손으로 5분 걸리는 계산(RSA 큰 수, 다익스트라 10노드)은 내지 않는다",
    "소재": "특정 제품·벤더 이름을 쓰지 않는다. 프로토콜·표준 이름은 쓴다",
    "금지": "실제 법 조문 인용 · 특정 기업 제품 비교 · 실기 코딩(구현을 요구하는 문항)",
}
