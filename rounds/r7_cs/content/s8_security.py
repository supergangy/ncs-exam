# -*- coding: utf-8 -*-
# 정보보안 36~40 (전산 1회)
#
# 근거 — README 2절. 후기 16건(11.7%) → 5문항.
#
# 배분 — 블루프린트(`bank/BLUEPRINT_cs.md`)의 다섯 갈래를 하나씩 —
#   대칭·공개키 2 · 해시·서명 1 · 접근통제 1 · 웹취약점 1.
#   네트워크 보안은 s5(방화벽·계층)에서 이미 건드렸다.
#
#   은행 36문항이 RSA·XOR·ALE·ACL·BLP·Biba 를 덮고 있어 그 밖을 골랐다 —
#   은행에는 **디피-헬만**도, **오류 전파**도, **전자봉투**도, **RBAC 세션**도 없다.
#
#   36 암호     — 디피-헬만. 지수를 직접 굴린다
#   37 암호     — CBC 에서 한 블록이 깨지면 어디까지 번지나
#   38 전자서명  — 전자봉투. 세션키를 누구의 키로 감싸나
#   39 접근통제  — RBAC. 가진 역할이 아니라 **켠 역할**이 권한을 준다
#   40 웹취약점  — 공격과 대책 짝짓기 (이 과목의 개념형 1문항)
#
# 36·39 는 `verify.py` 가 다시 구한다. 37·38 은 표준에 값이 고정된 문항이다.
#
# 정답 — ① ② ④ ③ ⑤
BLOCKS = [
    # ═══════════════════════════════════════════════════════════════
    # 36 디피-헬만
    #   p=23, g=5, a=6 → A=5^6 mod 23 = 8
    #                b=15 → B=5^15 mod 23 = 19
    #   공유키 = B^a mod p = 19^6 mod 23 = 2 ( = A^b mod p = 8^15 mod 23 )
    # ═══════════════════════════════════════════════════════════════
    {
        "area": "정보보안",
        "lead": None,
        "passage": None,
        "questions": [
            {
                "type": "암호",
                "stem": "두 사람이 나눠 갖게 되는 공유 비밀키는?",
                "material": (
                    '<div class="box">'
                    '<div class="box-title">&lt;디피-헬만 키 교환&gt;</div>'
                    '<table class="data">'
                    "<tr><th>공개된 값</th><td>소수 p = 23 · 생성원 g = 5</td></tr>"
                    "<tr><th>갑</th><td>비밀수 a = 6 → 보낸 값 A = 8</td></tr>"
                    "<tr><th>을</th><td>비밀수 b = 15 → 보낸 값 B = 19</td></tr>"
                    "</table>"
                    '<p class="note">※ 보내는 값은 g<sup>비밀수</sup> mod p 이고, '
                    "공유키는 (받은 값)<sup>자기 비밀수</sup> mod p 다.</p>"
                    "</div>"
                ),
                "choices": [
                    "2",
                    "4",
                    "8",
                    "16",
                    "19",
                ],
                "answer": 1,
                "explain": (
                    "<p>갑은 을이 보낸 19를 자기 비밀수 6으로 거듭제곱한다.</p>"
                    "<p>19² = 361 = 23 × 15 + 16 → <strong>16</strong><br>"
                    "19³ = 16 × 19 = 304 = 23 × 13 + 5 → <strong>5</strong><br>"
                    "19⁶ = (19³)² = 5² = 25 = 23 + 2 → <strong>2</strong></p>"
                    "<p>을도 같은 값에 이른다. 갑이 보낸 8을 자기 비밀수 15로 거듭제곱하면 "
                    "8<sup>15</sup> mod 23 = <strong>2</strong> 다.</p>"
                    "<p>둘 다 g<sup>ab</sup> mod p 를 구하는 셈이라 순서가 달라도 값이 같다. "
                    "엿듣는 쪽은 p · g · A · B 를 다 보지만, 거기서 a나 b를 되찾으려면 "
                    "<strong>이산로그를 풀어야</strong> 한다. 그것이 이 방식이 기대는 어려움이다.</p>"
                ),
                "each": [
                    "① (정답) 19⁶ mod 23 = 2. 갑이 보낸 8로 계산해도 8¹⁵ mod 23 = 2 로 같다.",
                    "② 4는 5⁴ mod 23 의 값이다. 생성원을 그대로 거듭제곱했다.",
                    "③ 8은 갑이 **보낸 값**이다. 공유키가 아니라 공개된 값이다.",
                    "④ 16은 19² mod 23 으로, 거듭제곱을 6이 아니라 2까지만 한 값이다.",
                    "⑤ 19는 을이 보낸 값이다. 그것을 자기 비밀수로 한 번 더 거듭제곱해야 한다.",
                ],
            },
        ],
    },

    # ═══════════════════════════════════════════════════════════════
    # 37 CBC 오류 전파 — 깨진 블록과 그 다음 블록, 둘까지
    # ═══════════════════════════════════════════════════════════════
    {
        "area": "정보보안",
        "lead": None,
        "passage": None,
        "questions": [
            {
                "type": "암호",
                "stem": "복호했을 때 원문과 달라지는 평문 블록을 모두 고른 것은?",
                "material": (
                    '<div class="box">'
                    '<div class="box-title">&lt;상황&gt;</div>'
                    "<p>평문을 다섯 블록(P1 ~ P5)으로 나눠 <strong>CBC 모드</strong>로 "
                    "암호화해 보냈다. 전송 중 <strong>C3의 한 비트가 뒤집혔고</strong>, "
                    "나머지 블록은 온전하다.</p>"
                    '<p class="note">※ CBC 복호는 P<sub>i</sub> = '
                    "D(C<sub>i</sub>) ⊕ C<sub>i−1</sub> 이다.</p>"
                    "</div>"
                ),
                "choices": [
                    "P3", "P3, P4", "P2, P3", "P3, P4, P5", "P3부터 P5까지 모두",
                ],
                "answer": 2,
                "explain": (
                    "<p>복호식에 C3가 나오는 자리를 찾으면 된다. C3는 <strong>두 곳</strong>에 "
                    "쓰인다.</p>"
                    '<table class="data">'
                    "<tr><th>블록</th><th>복호식</th><th>C3를 쓰나</th><th>결과</th></tr>"
                    "<tr><td>P2</td><td>D(C2) ⊕ C1</td><td>아니다</td><td>온전</td></tr>"
                    "<tr><td>P3</td><td>D(C3) ⊕ C2</td><td><strong>쓴다</strong></td>"
                    "<td>블록 전체가 엉킨다</td></tr>"
                    "<tr><td>P4</td><td>D(C4) ⊕ <strong>C3</strong></td>"
                    "<td><strong>쓴다</strong></td><td>같은 자리의 비트 하나만 뒤집힌다</td></tr>"
                    "<tr><td>P5</td><td>D(C5) ⊕ C4</td><td>아니다</td><td>온전</td></tr>"
                    "</table>"
                    "<p>D(C3)는 블록 암호를 거치므로 한 비트만 틀려도 <strong>블록 전체가 "
                    "알아볼 수 없게</strong> 된다. 반면 P4에서 C3는 XOR로 쓰일 뿐이라 "
                    "<strong>뒤집힌 그 비트 하나만</strong> 옮아간다.</p>"
                    "<p>오류는 <strong>두 블록에서 멈춘다.</strong> 뒤로 무한히 번지지 않는 것이 "
                    "CBC의 성질이다.</p>"
                ),
                "each": [
                    "① P4도 어긋난다. 복호할 때 C3를 XOR 값으로 쓴다.", "② (정답) P3는 블록 전체가, P4는 비트 하나가 어긋난다.", "③ P2는 C1과 C2만 쓴다. 오류는 **뒤로** 번지지 앞으로 가지 않는다.", "④ P5는 C4와 C5만 쓴다. C3와 무관하다.", "⑤ 끝까지 번지는 것은 암호화할 때의 이야기다. "
                    "**복호는 두 블록에서 멈춘다.**",
                ],
            },
        ],
    },

    # ═══════════════════════════════════════════════════════════════
    # 38 전자봉투 — 세션키를 받는 사람의 공개키로 감싼다
    # ═══════════════════════════════════════════════════════════════
    {
        "area": "정보보안",
        "lead": None,
        "passage": None,
        "questions": [
            {
                "type": "전자서명",
                "stem": "세션키를 감싸는 데 쓰는 키와 그 이유로 옳은 것은?",
                "material": (
                    '<div class="box">'
                    '<div class="box-title">&lt;상황&gt;</div>'
                    "<p>갑이 을에게 큰 파일을 남이 못 보게 보내려 한다. "
                    "파일 자체는 <strong>그 자리에서 만든 대칭키(세션키)</strong>로 암호화하고, "
                    "그 세션키를 따로 감싸 함께 보낸다(전자봉투).</p>"
                    '<p class="note">※ 갑과 을은 서로의 공개키를 알고 있고, 미리 나눠 가진 '
                    "대칭키는 없다.</p>"
                    "</div>"
                ),
                "choices": [
                    "갑의 개인키 — 갑이 보냈다는 것을 증명할 수 있으므로",
                    "갑의 공개키 — 갑만 다시 풀 수 있게 하려고",
                    "을의 개인키 — 을만 풀 수 있게 하려고",
                    "을의 공개키 — 을만 풀 수 있게 하려고",
                    "갑과 을이 미리 나눠 가진 대칭키 — 공개키보다 계산이 빠르므로",
                ],
                "answer": 4,
                "explain": (
                    "<p>공개키로 잠근 것은 <strong>짝이 되는 개인키로만</strong> 열린다. "
                    "을의 개인키는 을만 갖고 있으므로, 을의 공개키로 감싸면 을만 풀 수 있다.</p>"
                    '<table class="data">'
                    "<tr><th>차례</th><th>갑이 하는 일</th><th>을이 하는 일</th></tr>"
                    "<tr><td>1</td><td>세션키를 하나 만든다</td><td></td></tr>"
                    "<tr><td>2</td><td>파일을 세션키로 암호화</td><td></td></tr>"
                    "<tr><td>3</td><td>세션키를 <strong>을의 공개키</strong>로 암호화</td>"
                    "<td></td></tr>"
                    "<tr><td>4</td><td>둘을 함께 보낸다</td>"
                    "<td>자기 개인키로 세션키를 푼다</td></tr>"
                    "<tr><td>5</td><td></td><td>그 세션키로 파일을 푼다</td></tr>"
                    "</table>"
                    "<p>큰 파일까지 공개키로 암호화하지 않는 이유는 <strong>느리기 때문</strong>이다. "
                    "빠른 대칭키로 본문을 잠그고, 그 열쇠만 공개키로 감싼다 — "
                    "두 방식의 장점을 나눠 쓰는 것이다.</p>"
                    "<p>갑이 보냈다는 것까지 보이려면 <strong>전자서명을 따로 붙인다.</strong> "
                    "전자봉투는 비밀을 지키는 장치이지 신원을 밝히는 장치가 아니다.</p>"
                ),
                "each": [
                    "① 갑의 개인키로 감싼 것은 갑의 공개키로 누구나 풀 수 있다. "
                    "그것은 서명이지 비밀 유지가 아니다.",
                    "② 갑의 공개키로 잠그면 갑의 개인키로만 열린다. 을이 못 푼다.",
                    "③ 을의 개인키는 을만 갖고 있다. 갑이 쓸 수 없다.",
                    "④ (정답) 을의 공개키로 감싸면 짝이 되는 을의 개인키로만 열린다.",
                    "⑤ 미리 나눠 가진 대칭키가 없다. 그 키를 안전하게 나누는 문제를 "
                    "풀려고 전자봉투를 쓰는 것이다.",
                ],
            },
        ],
    },

    # ═══════════════════════════════════════════════════════════════
    # 39 RBAC — 켠 역할만 권한을 준다
    #   김: 조회자만 켬 → 읽기 / 이: 편집자 → 쓰기 / 박: 관리자 → 쓰기 / 최: 조회자
    # ═══════════════════════════════════════════════════════════════
    {
        "area": "정보보안",
        "lead": None,
        "passage": None,
        "questions": [
            {
                "type": "접근통제",
                "stem": "지금 이 순간 문서에 '쓰기'를 할 수 있는 사람을 모두 고르면?",
                "material": (
                    '<div class="box">'
                    '<div class="box-title">&lt;역할과 권한&gt;</div>'
                    '<table class="data">'
                    "<tr><th>역할</th><th>권한</th></tr>"
                    "<tr><td>조회자</td><td>읽기</td></tr>"
                    "<tr><td>편집자</td><td>읽기 · 쓰기</td></tr>"
                    "<tr><td>관리자</td><td>읽기 · 쓰기 · 삭제 · 권한부여</td></tr>"
                    "</table>"
                    '<div class="box-title">&lt;사용자&gt;</div>'
                    '<table class="data">'
                    "<tr><th>사용자</th><th>맡은 역할</th><th>지금 세션에서 켠 역할</th></tr>"
                    "<tr><td>김대리</td><td>조회자 · 편집자</td><td>조회자</td></tr>"
                    "<tr><td>이주임</td><td>편집자</td><td>편집자</td></tr>"
                    "<tr><td>박과장</td><td>관리자</td><td>관리자</td></tr>"
                    "<tr><td>최사원</td><td>조회자</td><td>조회자</td></tr>"
                    "</table>"
                    '<p class="note">※ RBAC에서 권한은 <strong>세션에서 켠 역할</strong>을 '
                    "통해서만 주어진다.</p>"
                    "</div>"
                ),
                "choices": [
                    "박과장",
                    "이주임",
                    "이주임, 박과장",
                    "김대리, 이주임, 박과장",
                    "김대리, 이주임, 박과장, 최사원",
                ],
                "answer": 3,
                "explain": (
                    "<p>맡은 역할이 아니라 <strong>지금 켠 역할</strong>이 권한을 준다.</p>"
                    '<table class="data">'
                    "<tr><th>사용자</th><th>켠 역할</th><th>지금 갖는 권한</th>"
                    "<th>쓰기</th></tr>"
                    "<tr><td>김대리</td><td>조회자</td><td>읽기</td><td>×</td></tr>"
                    "<tr><td>이주임</td><td>편집자</td><td>읽기 · 쓰기</td>"
                    "<td><strong>○</strong></td></tr>"
                    "<tr><td>박과장</td><td>관리자</td><td>읽기 · 쓰기 · 삭제 · 권한부여</td>"
                    "<td><strong>○</strong></td></tr>"
                    "<tr><td>최사원</td><td>조회자</td><td>읽기</td><td>×</td></tr>"
                    "</table>"
                    "<p>김대리는 편집자 역할을 <strong>갖고는 있지만 켜지 않았다.</strong> "
                    "지금 이 순간에는 쓰기를 할 수 없고, 세션에서 편집자를 켜야 할 수 있다.</p>"
                    "<p>이것이 최소 권한 원칙을 세션 단위로 지키는 방법이다 — "
                    "읽기만 하려고 들어왔다면 쓰기 권한은 켜 두지 않는다. "
                    "실수로든 침입자에게든 그 권한이 쓰일 일을 없앤다.</p>"
                ),
                "each": [
                    "① 이주임도 편집자를 켜 두었다. 편집자에게는 쓰기 권한이 있다.",
                    "② 박과장의 관리자 역할에도 쓰기가 들어 있다.",
                    "③ (정답) 이주임(편집자)과 박과장(관리자) 둘이다.",
                    "④ 김대리는 편집자 역할을 갖고 있지만 지금은 조회자만 켰다. "
                    "**갖는 것과 켜는 것은 다르다.**",
                    "⑤ 최사원은 조회자 역할만 갖고 있다. 켜고 말고를 떠나 쓰기 권한이 없다.",
                ],
            },
        ],
    },

    # ═══════════════════════════════════════════════════════════════
    # 40 공격과 대책 — ⑤ 가 오히려 세션 고정 공격을 부른다
    # ═══════════════════════════════════════════════════════════════
    {
        "area": "정보보안",
        "lead": None,
        "passage": None,
        "questions": [
            {
                "type": "웹취약점",
                "stem": "공격과 그것을 막는 방법을 짝지은 것으로 옳지 않은 것은?",
                "material": (
                    '<div class="box">'
                    '<p class="note">※ 웹 애플리케이션을 기준으로 한다.</p>'
                    "</div>"
                ),
                "choices": [
                    "SQL 인젝션 — 질의문에 값을 이어 붙이지 않고 매개변수로 넘긴다",
                    "XSS — 화면에 내보낼 때 HTML 특수문자를 이스케이프한다",
                    "CSRF — 요청마다 예측할 수 없는 토큰을 붙이고 서버가 확인한다",
                    "경로 조작 — 입력받은 경로를 정규화한 뒤 허용된 뿌리 안에 있는지 확인한다",
                    "세션 고정 — 로그인에 성공하면 들어올 때 쓰던 세션 ID를 그대로 이어 쓴다",
                ],
                "answer": 5,
                "explain": (
                    "<p>세션 고정 공격은 <strong>공격자가 미리 만들어 둔 세션 ID를 "
                    "피해자에게 쥐여 주는</strong> 수법이다. 피해자가 그 ID로 로그인하면 "
                    "같은 ID를 들고 있는 공격자도 로그인된 세션을 쓰게 된다.</p>"
                    "<p>막는 방법은 그 반대다 — <strong>로그인에 성공하는 순간 세션 ID를 "
                    "새로 발급하고</strong> 예전 것을 버린다. 그러면 공격자가 쥐여 준 ID는 "
                    "로그인과 이어지지 않는다.</p>"
                    "<p>「그대로 이어 쓴다」는 것은 대책이 아니라 <strong>취약점 그 자체</strong>다.</p>"
                    '<table class="data">'
                    "<tr><th>공격</th><th>핵심 대책</th></tr>"
                    "<tr><td>SQL 인젝션</td><td>매개변수화 질의 — 값이 코드로 읽히지 않게</td></tr>"
                    "<tr><td>XSS</td><td>출력 이스케이프 — 값이 태그로 읽히지 않게</td></tr>"
                    "<tr><td>CSRF</td><td>토큰 — 남의 사이트에서 만든 요청을 가려내게</td></tr>"
                    "<tr><td>경로 조작</td><td>정규화 후 허용 범위 확인</td></tr>"
                    "<tr><td>세션 고정</td><td>로그인 성공 시 세션 ID 재발급</td></tr>"
                    "</table>"
                ),
                "each": [
                    "① 맞다. 값을 이어 붙이지 않으면 입력이 질의문의 구조를 바꿀 수 없다.",
                    "② 맞다. 저장할 때가 아니라 **내보낼 때** 이스케이프하는 것이 맞다.",
                    "③ 맞다. 남의 사이트에서는 그 토큰을 알아낼 수 없다.",
                    "④ 맞다. `../` 를 지우는 것만으로는 모자라고 정규화한 뒤 확인해야 한다.",
                    "⑤ (정답) 틀렸다. 그대로 이어 쓰면 오히려 세션 고정을 당한다. "
                    "로그인에 성공하면 세션 ID를 **새로 발급**해야 한다.",
                ],
            },
        ],
    },
]
