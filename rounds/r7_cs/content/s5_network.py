# -*- coding: utf-8 -*-
# 네트워크 17~24 (전산 1회)
#
# 근거 — README 2절. 후기 30건(21.9%) → 8문항. 가장 큰 몫이다.
#   후기가 이름을 댄 것 — 「네트워크 7계층」 · 「네트워크의 종류」 · 「로컬네트워크효과」
#
# 배분 — 서브네팅 1 · 전송계층 1 · 라우팅 1 · 계층 1 · 응용 1 ·
#        데이터링크 1 · 성능 1 · IP 1. **한 갈래에 몰지 않았다.**
#   은행 40문항이 이미 두터워, 겹치지 않는 각도만 골랐다 —
#   은행은 고정 크기 서브네팅인데 여기는 **VLSM**,
#   은행은 혼잡 윈도인데 여기는 **수신 윈도와의 최솟값**,
#   은행은 스위치 플러딩의 뜻인데 여기는 **학습 과정을 따라가게** 한다.
#
#   17 서브네팅   — VLSM. 큰 부서부터 잘라 내려간다
#   18 전송계층   — min(cwnd, rwnd) 에서 미확인분을 뺀다
#   19 라우팅     — 다익스트라. 링크가 끊기면 값이 어떻게 되나
#   20 계층       — 캡슐화. 물리계층은 주소를 붙이지 않는다
#   21 응용계층   — DHCP DORA 와 서버 포트
#   22 데이터링크 — 스위치가 언제 플러딩을 멈추나
#   23 성능       — 슬라이딩 윈도 이용률
#   24 IP        — 단편화. 오프셋은 8로 나눈 값이다
#
# 20·21 은 표준(RFC)에 값이 고정된 문항이고 나머지 여섯은 계산이다.
#
# 정답 — ② ① ③ ⑤ ① ① ② ②
BLOCKS = [
    # ═══════════════════════════════════════════════════════════════
    # 17 VLSM — 192.168.100.0/24
    #   영업 100대 → /25  .0   ~ .127
    #   개발  50대 → /26  .128 ~ .191
    #   총무  25대 → /27  .192 ~ .223
    #   인사  10대 → /28  .224 ~ .239
    #   남는 것 .240 ~ .255 = 16개
    # ═══════════════════════════════════════════════════════════════
    {
        "area": "네트워크",
        "lead": None,
        "passage": None,
        "questions": [
            {
                "type": "서브네팅",
                "stem": "인사팀에 배정되는 네트워크 주소와, 어느 팀에도 배정되지 않고 남는 주소의 개수는?",
                "material": (
                    '<div class="box">'
                    '<div class="box-title">&lt;주소 배정&gt;</div>'
                    "<p>192.168.100.0/24 하나를 받아 네 팀에 나눈다. "
                    "<strong>필요한 호스트가 많은 팀부터</strong> 차례로, "
                    "주소가 낮은 쪽부터 잘라 준다.</p>"
                    '<table class="data">'
                    "<tr><th>팀</th><th>필요한 호스트</th></tr>"
                    "<tr><td>영업팀</td><td>100대</td></tr>"
                    "<tr><td>개발팀</td><td>50대</td></tr>"
                    "<tr><td>총무팀</td><td>25대</td></tr>"
                    "<tr><td>인사팀</td><td>10대</td></tr>"
                    "</table>"
                    '<p class="note">※ 서브넷마다 네트워크 주소와 브로드캐스트 주소는 '
                    "호스트에 쓸 수 없다.</p>"
                    "</div>"
                ),
                "choices": [
                    "192.168.100.192/28 · 16개",
                    "192.168.100.224/28 · 16개",
                    "192.168.100.224/28 · 14개",
                    "192.168.100.224/29 · 24개",
                    "192.168.100.240/28 · 0개",
                ],
                "answer": 2,
                "explain": (
                    "<p>필요한 호스트 수를 담을 수 있는 <strong>가장 작은 크기</strong>를 고른다. "
                    "쓸 수 있는 호스트는 (주소 수 − 2) 다.</p>"
                    '<table class="data">'
                    "<tr><th>팀</th><th>필요</th><th>프리픽스</th><th>주소 수</th>"
                    "<th>쓸 수 있는 호스트</th><th>구간</th></tr>"
                    "<tr><td>영업</td><td>100</td><td>/25</td><td>128</td><td>126</td>"
                    "<td>.0 ~ .127</td></tr>"
                    "<tr><td>개발</td><td>50</td><td>/26</td><td>64</td><td>62</td>"
                    "<td>.128 ~ .191</td></tr>"
                    "<tr><td>총무</td><td>25</td><td>/27</td><td>32</td><td>30</td>"
                    "<td>.192 ~ .223</td></tr>"
                    "<tr><td>인사</td><td>10</td><td><strong>/28</strong></td><td>16</td>"
                    "<td>14</td><td><strong>.224 ~ .239</strong></td></tr>"
                    "</table>"
                    "<p>인사팀은 <strong>192.168.100.224/28</strong> 이다.</p>"
                    "<p>.240 부터 .255 까지가 남는다. 모두 <strong>16개</strong>다.</p>"
                    "<p>큰 것부터 자르는 이유는 조각이 어긋나지 않게 하려는 것이다. "
                    "작은 것부터 자르면 /25 를 놓을 자리가 중간에 걸려 못 쓰게 된다.</p>"
                ),
                "each": [
                    "① .192는 총무팀(/27)의 자리다. 인사팀은 그 다음이다.",
                    "② (정답) 인사팀은 .224/28, 남는 주소는 .240 ~ .255 로 16개다.",
                    "③ 14는 남는 **주소 수**가 아니라 /28 에서 쓸 수 있는 **호스트 수**다. "
                    ".240 이후는 배정 자체가 없으므로 네트워크·브로드캐스트를 뺄 이유가 없다.",
                    "④ /29 는 주소가 8개뿐이라 쓸 수 있는 호스트가 6대다. 10대를 담지 못한다.",
                    "⑤ .240/28 은 남는 자리다. 인사팀은 그 앞 칸을 받는다.",
                ],
            },
        ],
    },

    # ═══════════════════════════════════════════════════════════════
    # 18 송신 가능 바이트 = min(cwnd, rwnd) - 미확인
    #   min(12000, 8000) = 8000, 8000 - 5000 = 3000
    # ═══════════════════════════════════════════════════════════════
    {
        "area": "네트워크",
        "lead": None,
        "passage": None,
        "questions": [
            {
                "type": "전송계층",
                "stem": "이 시점에 송신 측이 곧바로 더 보낼 수 있는 데이터의 양은?",
                "material": (
                    '<div class="box">'
                    '<div class="box-title">&lt;TCP 연결 상태&gt;</div>'
                    '<table class="data">'
                    "<tr><th>혼잡 윈도(cwnd)</th><td>12,000바이트</td></tr>"
                    "<tr><th>수신 측이 알린 윈도(rwnd)</th><td>8,000바이트</td></tr>"
                    "<tr><th>보냈지만 아직 확인받지 못한 양</th><td>5,000바이트</td></tr>"
                    "</table>"
                    "</div>"
                ),
                "choices": [
                    "3,000바이트",
                    "4,000바이트",
                    "7,000바이트",
                    "8,000바이트",
                    "15,000바이트",
                ],
                "answer": 1,
                "explain": (
                    "<p>송신 윈도는 두 윈도의 <strong>작은 쪽</strong>이다.</p>"
                    "<p>min(12,000, 8,000) = <strong>8,000바이트</strong></p>"
                    "<p>그 가운데 5,000바이트는 이미 나가서 확인을 기다리는 중이다.</p>"
                    "<p>8,000 − 5,000 = <strong>3,000바이트</strong></p>"
                    "<p>두 윈도는 서로 다른 것을 막는다. cwnd는 <strong>망이 밀리는 것</strong>을, "
                    "rwnd는 <strong>받는 쪽 버퍼가 넘치는 것</strong>을 막는다. "
                    "둘 중 하나라도 넘으면 안 되므로 작은 쪽을 따른다.</p>"
                ),
                "each": [
                    "① (정답) min(12,000, 8,000) − 5,000 = 3,000바이트.",
                    "② 4,000은 두 윈도의 차(12,000 − 8,000)다. 윈도는 빼는 것이 아니라 "
                    "작은 쪽을 고르는 것이다.",
                    "③ 7,000은 큰 쪽인 cwnd에서 미확인분을 뺀 값이다. 수신 측 버퍼를 넘긴다.",
                    "④ 8,000은 송신 윈도 자체다. 미확인 5,000바이트가 이미 그 안을 쓰고 있다.",
                    "⑤ 15,000은 두 윈도를 더한 값이다. 두 제약은 더해지지 않는다.",
                ],
            },
        ],
    },

    # ═══════════════════════════════════════════════════════════════
    # 19 다익스트라
    #   A-B2 A-C5 B-C1 B-D7 C-D3 C-E8 D-E2 D-F6 E-F1
    #   A→F : A-B-C-D-E-F = 2+1+3+2+1 = 9
    #   D-E 끊김 : A-B-C-D-F = 12, A-B-C-E-F = 12 → 12
    # ═══════════════════════════════════════════════════════════════
    {
        "area": "네트워크",
        "lead": None,
        "passage": None,
        "questions": [
            {
                "type": "라우팅",
                "stem": "A에서 F까지의 최소 비용과, D–E 링크가 끊긴 뒤의 최소 비용을 바르게 짝지은 것은?",
                "material": (
                    '<div class="box">'
                    '<div class="box-title">&lt;링크 비용&gt;</div>'
                    '<table class="data">'
                    "<tr><th>링크</th><th>비용</th><th>링크</th><th>비용</th>"
                    "<th>링크</th><th>비용</th></tr>"
                    "<tr><td>A–B</td><td>2</td><td>B–D</td><td>7</td>"
                    "<td>D–E</td><td>2</td></tr>"
                    "<tr><td>A–C</td><td>5</td><td>C–D</td><td>3</td>"
                    "<td>D–F</td><td>6</td></tr>"
                    "<tr><td>B–C</td><td>1</td><td>C–E</td><td>8</td>"
                    "<td>E–F</td><td>1</td></tr>"
                    "</table>"
                    '<p class="note">※ 모든 링크는 양방향이고 비용이 같다.</p>'
                    "</div>"
                ),
                "choices": [
                    "9 · 9", "9 · 11", "9 · 12", "10 · 12", "12 · 12",
                ],
                "answer": 3,
                "explain": (
                    "<p>A에서 출발해 값이 확정되는 차례로 적으면 이렇다.</p>"
                    '<table class="data">'
                    "<tr><th>노드</th><th>최소 비용</th><th>경로</th></tr>"
                    "<tr><td>B</td><td>2</td><td>A–B</td></tr>"
                    "<tr><td>C</td><td>3</td><td>A–B–C (2+1). 직통 A–C 5보다 싸다</td></tr>"
                    "<tr><td>D</td><td>6</td><td>A–B–C–D (3+3)</td></tr>"
                    "<tr><td>E</td><td>8</td><td>A–B–C–D–E (6+2)</td></tr>"
                    "<tr><td>F</td><td><strong>9</strong></td>"
                    "<td>A–B–C–D–E–F (8+1)</td></tr>"
                    "</table>"
                    "<p>D–E가 끊기면 E로 가는 값싼 길이 사라진다. F로 가는 길은 둘만 남는다.</p>"
                    "<p>A–B–C–D–F = 3 + 3 + 6 = <strong>12</strong><br>"
                    "A–B–C–E–F = 3 + 8 + 1 = <strong>12</strong></p>"
                    "<p>둘 다 12라 최소 비용은 <strong>12</strong>다. 링크 하나가 빠지면서 "
                    "3만큼 비싸졌다.</p>"
                ),
                "each": [
                    "① 끊긴 뒤에도 9가 나오려면 D–E를 지나야 한다. 그 링크가 없다.", "② 11은 A에서 E까지의 값이다(A–B–C–E = 3+8). F까지 1이 더 든다.", "③ (정답) 9와 12.", "④ 10은 어느 경로로도 나오지 않는다. A–C를 직통 5로 잡으면 "
                    "5+3+6 = 14 로 오히려 늘어난다.", "⑤ 12는 D–E를 쓰지 않은 값이다. 링크가 살아 있을 때는 그보다 싼 9가 있다.",
                ],
            },
        ],
    },

    # ═══════════════════════════════════════════════════════════════
    # 20 캡슐화 — 물리계층은 주소를 붙이지 않는다
    # ═══════════════════════════════════════════════════════════════
    {
        "area": "네트워크",
        "lead": None,
        "passage": None,
        "questions": [
            {
                "type": "계층",
                "stem": "송신 측에서 데이터가 아래 계층으로 내려갈 때에 대한 설명으로 옳지 않은 것은?",
                "material": (
                    '<div class="box">'
                    '<p class="note">※ TCP/IP 프로토콜 스택을 기준으로 한다.</p>'
                    "</div>"
                ),
                "choices": [
                    "전송 계층에서 포트 번호가 붙고, 그 결과를 세그먼트라고 한다",
                    "네트워크 계층에서 IP 주소가 붙고, 그 결과를 패킷이라고 한다",
                    "데이터링크 계층에서 MAC 주소가 붙고, 그 결과를 프레임이라고 한다",
                    "데이터링크 계층에서는 뒤쪽에 오류검사 필드(FCS)도 함께 붙는다",
                    "물리 계층에서 포트 번호가 한 번 더 붙어 비트열이 된다",
                ],
                "answer": 5,
                "explain": (
                    "<p>계층마다 자기 헤더를 붙이며 내려간다. 이것을 캡슐화라고 한다.</p>"
                    '<table class="data">'
                    "<tr><th>계층</th><th>붙는 것</th><th>결과의 이름</th></tr>"
                    "<tr><td>전송</td><td>포트 번호</td><td>세그먼트(TCP)</td></tr>"
                    "<tr><td>네트워크</td><td>IP 주소</td><td>패킷</td></tr>"
                    "<tr><td>데이터링크</td><td>MAC 주소 · 뒤에 FCS</td><td>프레임</td></tr>"
                    "<tr><td>물리</td><td><strong>아무 주소도 붙지 않는다</strong></td>"
                    "<td>비트</td></tr>"
                    "</table>"
                    "<p>물리 계층은 프레임을 <strong>전기·빛 신호로 바꿔 내보낼 뿐</strong>이다. "
                    "여기에는 헤더도 주소도 없다. 포트 번호는 전송 계층에서 이미 한 번 붙었고 "
                    "다시 붙을 일이 없다.</p>"
                    "<p>데이터링크 계층만 <strong>앞뒤로</strong> 붙인다. 앞에는 헤더(MAC 주소 등), "
                    "뒤에는 트레일러(FCS)다.</p>"
                ),
                "each": [
                    "① 맞다. 포트 번호는 전송 계층의 몫이고 결과는 세그먼트다.",
                    "② 맞다. IP 주소는 네트워크 계층에서 붙고 결과는 패킷이다.",
                    "③ 맞다. MAC 주소는 데이터링크 계층에서 붙고 결과는 프레임이다.",
                    "④ 맞다. 데이터링크 계층은 앞에 헤더, 뒤에 FCS를 붙인다.",
                    "⑤ (정답) 틀렸다. 물리 계층은 주소를 붙이지 않는다. "
                    "프레임을 신호로 바꿔 보낼 뿐이다.",
                ],
            },
        ],
    },

    # ═══════════════════════════════════════════════════════════════
    # 21 DHCP DORA + 서버 포트 67
    # ═══════════════════════════════════════════════════════════════
    {
        "area": "네트워크",
        "lead": None,
        "passage": None,
        "questions": [
            {
                "type": "응용계층",
                "stem": "DHCP로 IP 주소를 처음 받을 때 오가는 메시지의 차례와, 서버가 듣는 포트 번호는?",
                "material": (
                    '<div class="box">'
                    '<p class="note">※ 주소를 아직 하나도 갖고 있지 않은 단말이 막 켜진 상황이다.</p>'
                    "</div>"
                ),
                "choices": [
                    "DISCOVER → OFFER → REQUEST → ACK · 67번",
                    "DISCOVER → OFFER → REQUEST → ACK · 68번",
                    "DISCOVER → REQUEST → OFFER → ACK · 67번",
                    "OFFER → DISCOVER → REQUEST → ACK · 68번",
                    "REQUEST → OFFER → DISCOVER → ACK · 67번",
                ],
                "answer": 1,
                "explain": (
                    "<p>네 메시지의 앞 글자를 따 <strong>DORA</strong>라고 부른다.</p>"
                    '<table class="data">'
                    "<tr><th>차례</th><th>메시지</th><th>보내는 쪽</th><th>하는 일</th></tr>"
                    "<tr><td>1</td><td>DISCOVER</td><td>단말</td>"
                    "<td>「서버 있습니까」 — 브로드캐스트</td></tr>"
                    "<tr><td>2</td><td>OFFER</td><td>서버</td>"
                    "<td>「이 주소를 쓰시겠습니까」</td></tr>"
                    "<tr><td>3</td><td>REQUEST</td><td>단말</td>"
                    "<td>「그것으로 하겠습니다」 — 역시 브로드캐스트</td></tr>"
                    "<tr><td>4</td><td>ACK</td><td>서버</td><td>임대 확정</td></tr>"
                    "</table>"
                    "<p>단말이 아직 주소가 없으므로 DISCOVER는 브로드캐스트로 나간다. "
                    "REQUEST도 브로드캐스트인데, 여러 서버가 OFFER를 보냈을 때 "
                    "<strong>고르지 않은 서버들에게도 알려 주소를 풀어 주게</strong> 하려는 것이다.</p>"
                    "<p>포트는 <strong>서버 67 · 단말 68</strong>이다(UDP).</p>"
                ),
                "each": [
                    "① (정답) DISCOVER → OFFER → REQUEST → ACK, 서버 포트는 67번이다.",
                    "② 68번은 단말이 듣는 포트다. 서버가 ACK를 보낼 때 목적지로 쓴다.",
                    "③ OFFER를 받아야 무엇을 REQUEST할지 정해진다. 순서가 뒤집혔다.",
                    "④ 서버는 DISCOVER를 받고 나서야 OFFER를 보낸다. 먼저 말을 걸지 않는다.",
                    "⑤ 주소를 모르는 단말이 REQUEST부터 보낼 수는 없다.",
                ],
            },
        ],
    },

    # ═══════════════════════════════════════════════════════════════
    # 22 스위치 MAC 학습
    #   A→B 플러딩(B 모름) / B→A 유니캐스트 / C→A 유니캐스트 / D→C 유니캐스트
    #   플러딩 1건, 테이블 4개
    # ═══════════════════════════════════════════════════════════════
    {
        "area": "네트워크",
        "lead": None,
        "passage": None,
        "questions": [
            {
                "type": "데이터링크",
                "stem": "플러딩되는 프레임의 개수와, 네 프레임이 모두 지나간 뒤 MAC 주소 테이블에 남는 항목 수는?",
                "material": (
                    '<div class="box">'
                    '<div class="box-title">&lt;상황&gt;</div>'
                    "<p>스위치의 포트 1·2·3·4에 각각 호스트 A·B·C·D가 붙어 있다. "
                    "MAC 주소 테이블은 <strong>비어 있는 상태에서 시작</strong>한다.</p>"
                    '<table class="data">'
                    "<tr><th>차례</th><th>보내는 쪽</th><th>받는 쪽</th></tr>"
                    "<tr><td>1</td><td>A</td><td>B</td></tr>"
                    "<tr><td>2</td><td>B</td><td>A</td></tr>"
                    "<tr><td>3</td><td>C</td><td>A</td></tr>"
                    "<tr><td>4</td><td>D</td><td>C</td></tr>"
                    "</table>"
                    '<p class="note">※ 네 프레임 모두 유니캐스트이고, 목적지 MAC 주소를 '
                    "제대로 담고 있다.</p>"
                    "</div>"
                ),
                "choices": [
                    "1개 · 4개",
                    "2개 · 4개",
                    "2개 · 3개",
                    "3개 · 4개",
                    "4개 · 4개",
                ],
                "answer": 1,
                "explain": (
                    "<p>스위치는 프레임이 들어올 때마다 <strong>보내는 쪽 주소를 배우고</strong>, "
                    "<strong>받는 쪽 주소를 찾아본다.</strong> 모르면 들어온 포트를 뺀 "
                    "모든 포트로 내보낸다(플러딩).</p>"
                    '<table class="data">'
                    "<tr><th>차례</th><th>배우는 것</th><th>목적지를 아나</th><th>처리</th></tr>"
                    "<tr><td>1 A→B</td><td>A = 포트1</td><td>B를 모른다</td>"
                    "<td><strong>플러딩</strong></td></tr>"
                    "<tr><td>2 B→A</td><td>B = 포트2</td><td>A = 포트1</td>"
                    "<td>포트1로만</td></tr>"
                    "<tr><td>3 C→A</td><td>C = 포트3</td><td>A = 포트1</td>"
                    "<td>포트1로만</td></tr>"
                    "<tr><td>4 D→C</td><td>D = 포트4</td><td>C = 포트3</td>"
                    "<td>포트3으로만</td></tr>"
                    "</table>"
                    "<p>플러딩은 <strong>첫 프레임 한 번</strong>뿐이다. "
                    "A가 B에게 보내는 순간 스위치는 A만 알 뿐 B가 어디 있는지 모른다. "
                    "그런데 B가 답장을 보내면서 자기 자리를 알려 주므로, 그 뒤로는 찾을 필요가 없다.</p>"
                    "<p>네 호스트가 모두 한 번씩 보냈으므로 테이블에는 <strong>4개</strong>가 남는다.</p>"
                ),
                "each": [
                    "① (정답) 플러딩은 첫 프레임 하나뿐이고, 테이블에는 A·B·C·D 4개가 남는다.",
                    "② 두 번째 프레임(B→A)은 플러딩되지 않는다. 첫 프레임이 이미 A의 자리를 "
                    "알려 주었다.",
                    "③ 넷이 모두 한 번씩 **보내는 쪽**이 되었다. 항목은 4개다.",
                    "④ 세 번째(C→A)도 목적지 A를 안다. 스위치는 목적지를 알면 그 포트로만 보낸다.",
                    "⑤ 스위치는 배운 것을 잊지 않는다(수명이 다하기 전까지). "
                    "모든 프레임을 플러딩하는 것은 허브다.",
                ],
            },
        ],
    },

    # ═══════════════════════════════════════════════════════════════
    # 23 슬라이딩 윈도 이용률
    #   Tf = 10,000 / 10^7 = 1ms
    #   Tf + 2Tp = 1 + 20 = 21ms
    #   4 × 1 / 21 = 19.0%
    # ═══════════════════════════════════════════════════════════════
    {
        "area": "네트워크",
        "lead": None,
        "passage": None,
        "questions": [
            {
                "type": "성능",
                "stem": "이 회선의 이용률은?",
                "material": (
                    '<div class="box">'
                    '<div class="box-title">&lt;회선 조건&gt;</div>'
                    '<table class="data">'
                    "<tr><th>전송 속도</th><td>10Mbps</td></tr>"
                    "<tr><th>프레임 크기</th><td>10,000비트</td></tr>"
                    "<tr><th>편도 전파지연</th><td>10ms</td></tr>"
                    "<tr><th>윈도 크기</th><td>프레임 4개</td></tr>"
                    "</table>"
                    '<p class="note">※ 1Mbps = 10<sup>6</sup>bps 로 본다. '
                    "확인응답의 전송 시간과 처리 시간은 없다고 본다.</p>"
                    "</div>"
                ),
                "choices": [
                    "4.8%",
                    "19.0%",
                    "20.0%",
                    "40.0%",
                    "100.0%",
                ],
                "answer": 2,
                "explain": (
                    "<p>먼저 프레임 하나를 회선에 밀어 넣는 데 걸리는 시간을 구한다.</p>"
                    "<p>10,000비트 ÷ 10,000,000bps = 0.001초 = <strong>1ms</strong></p>"
                    "<p>첫 프레임을 보내기 시작해 그 확인응답이 돌아올 때까지는</p>"
                    "<p>1ms(전송) + 10ms(가는 길) + 10ms(오는 길) = <strong>21ms</strong></p>"
                    "<p>그 21ms 동안 윈도 크기만큼, 곧 4개를 연달아 보낼 수 있다. "
                    "실제로 회선을 쓴 시간은 4 × 1 = 4ms 다.</p>"
                    "<p>4 ÷ 21 = 0.1904… → <strong>약 19.0%</strong></p>"
                    "<p>회선을 가득 채우려면 윈도가 21개는 되어야 한다. "
                    "<strong>거리가 멀수록 윈도를 키워야 하는 이유</strong>가 이것이다.</p>"
                ),
                "each": [
                    "① 4.8%는 윈도가 1일 때, 곧 정지-대기 방식의 이용률이다(1 ÷ 21).",
                    "② (정답) 4 × 1ms ÷ 21ms ≈ 19.0%.",
                    "③ 20.0%는 분모에서 전송 시간 1ms를 빼고 왕복 20ms만 쓴 값이다. "
                    "첫 프레임을 밀어 넣는 시간도 기다림에 든다.",
                    "④ 40.0%는 전파지연을 편도 5ms로 본 값이다. 왕복이므로 두 번 든다.",
                    "⑤ 100%가 되려면 확인응답이 돌아오기 전에 윈도를 다 못 쓰는 일이 "
                    "없어야 한다. 윈도가 21개는 되어야 한다.",
                ],
            },
        ],
    },

    # ═══════════════════════════════════════════════════════════════
    # 24 IPv4 단편화
    #   총 4,000 = 헤더 20 + 데이터 3,980 / MTU 1,500 → 조각당 데이터 1,480
    #   1,480 + 1,480 + 1,020 = 3,980 → 3조각
    #   마지막 오프셋 = 2,960 / 8 = 370
    # ═══════════════════════════════════════════════════════════════
    {
        "area": "네트워크",
        "lead": None,
        "passage": None,
        "questions": [
            {
                "type": "IP주소",
                "stem": "조각의 개수와 마지막 조각의 단편 오프셋 값은?",
                "material": (
                    '<div class="box">'
                    '<div class="box-title">&lt;단편화 상황&gt;</div>'
                    '<table class="data">'
                    "<tr><th>원본 데이터그램 전체 길이</th><td>4,000바이트</td></tr>"
                    "<tr><th>IP 헤더 길이</th><td>20바이트 (옵션 없음)</td></tr>"
                    "<tr><th>지나갈 링크의 MTU</th><td>1,500바이트</td></tr>"
                    "</table>"
                    '<p class="note">※ 조각의 데이터 부분은 8의 배수여야 한다.</p>'
                    '<p class="note">※ 단편 오프셋은 원본 데이터의 시작에서 몇 번째 바이트인지를 '
                    "<strong>8로 나눈 값</strong>으로 적는다.</p>"
                    "</div>"
                ),
                "choices": [
                    "3개 · 185", "3개 · 370", "3개 · 2,960", "4개 · 370", "4개 · 500",
                ],
                "answer": 2,
                "explain": (
                    "<p>실을 수 있는 데이터는 MTU에서 헤더를 뺀 만큼이다.</p>"
                    "<p>1,500 − 20 = 1,480바이트. 1,480은 8의 배수(8 × 185)라 그대로 쓴다.</p>"
                    "<p>나를 데이터는 4,000 − 20 = <strong>3,980바이트</strong>다.</p>"
                    '<table class="data">'
                    "<tr><th>조각</th><th>데이터</th><th>원본에서의 자리</th><th>오프셋</th></tr>"
                    "<tr><td>1</td><td>1,480</td><td>0 ~ 1,479</td><td>0 ÷ 8 = 0</td></tr>"
                    "<tr><td>2</td><td>1,480</td><td>1,480 ~ 2,959</td>"
                    "<td>1,480 ÷ 8 = 185</td></tr>"
                    "<tr><td>3</td><td>1,020</td><td>2,960 ~ 3,979</td>"
                    "<td>2,960 ÷ 8 = <strong>370</strong></td></tr>"
                    "</table>"
                    "<p>조각은 <strong>3개</strong>, 마지막 오프셋은 <strong>370</strong>이다.</p>"
                    "<p>헤더 20바이트는 <strong>조각마다 새로 붙는다.</strong> "
                    "그래서 마지막 조각의 전체 길이는 20 + 1,020 = 1,040바이트가 된다.</p>"
                ),
                "each": [
                    "① 185는 **두 번째** 조각의 오프셋이다.", "② (정답) 3개, 마지막 오프셋 370.", "③ 2,960은 바이트 단위 자리다. 오프셋 필드에는 8로 나눈 값을 적는다.", "④ 조각은 3개다. 4,000을 1,500으로 나눠 셋에 나머지가 있다고 보면 "
                    "4개가 되지만, 나르는 것은 헤더를 뺀 3,980이다.", "⑤ 500은 4,000 ÷ 8 로, 원본 전체를 8로 나눈 값이다. "
                    "오프셋은 **데이터 부분**의 자리를 가리킨다.",
                ],
            },
        ],
    },
]
