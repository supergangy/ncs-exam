# -*- coding: utf-8 -*-
"""제6회 — 서울교통공사 대비 (10영역 40문항 / 45분)

같은 사양이 **두 회차에서 확인**되었다(나라장터 조달 공고, 2025.2.8. 본사 ·
2026.1.24. 9호선). 40문항 / 45분 / 5지선다 / NCS 전영역 / 대행사 휴스테이션.

## 후기 빈도를 쓰지 않는다

후기 384건의 언급 빈도로는 「80문항 90분」이 최다였고 수리 43% 로 보였다.
**둘 다 설계 근거가 못 된다** — 후기의 문항 수는 인성검사를 포함하거나 다른
시험을 섞은 것이고, 영역 언급은 「기억에 남은 것」이지 출제 비중이 아니다.
후기는 **무엇이 나왔나**(소재·유형)에 쓰고 **몇 문항인가**는 시행 자료로 잡는다.

## 다른 회차와 다른 점

코레일(r2~r4)은 피셋 위주다. 여기는 **모듈이 35~40%** 다. 그리고 모듈이 다시
둘로 갈린다 — 산인공 표준 교재의 항목을 그대로 묻는 「산인공 모듈」과,
공문서 규칙·엑셀 함수처럼 실무 지식을 묻는 「응용 모듈」이다.

건보(r5)는 3영역 60문항이었다. 여기는 **10영역을 각 4문항으로 고르게** 나눈다 —
두 회차 모두 정확히 각 4문항이었다. 그래서 한 영역이라도 비면 회차가 성립하지 않는다.

## 2026-08-28 확인 — 계획서의 승인 항목 둘은 이미 해소됐다

README 5절이 물은 것 중 둘은 그 뒤에 답이 나왔다.

  (1) 자기개발·대인관계가 은행에 **0** 이라 20% 를 새 영역에서 써야 한다
      → 지금 **각 12문항**이 있다. 계획서가 요구한 주제(갈등해결 5형 ·
        임파워먼트 4차원 · 팔로워십 · 감정은행계좌 · 경청 · 경력개발 4유형 ·
        적극행정)를 그대로 덮는다. 회차 문항은 새로 쓰되 그 유형 어휘를 따른다.

  (2) 산인공 모듈을 개념 정의형으로 물으면 SPEC 2절과 부딪힌다
      → 은행의 24문항이 **이미 사례형**이다(「다음 상황에서 …에 해당하는 …은?」).
        같은 형으로 쓴다. 실제 시험도 그렇게 낸다.

회차 블루프린트와 근거는 같은 폴더의 README.md.
"""
BRAND = "서울교통공사 채용대비"
EXAM_TITLE = "NCS 봉투모의고사"
EXAM_ROUND = "실전모의고사 제6회"
FILE_TAG = "NCS_봉투모의고사_6회_서울교통공사"   # 출력 PDF 파일명 접두사
TOTAL_Q = 40
TOTAL_MIN = 45

# 번호는 이 선언 순서로 매겨진다 (`build.py` 가 glob 이 아니라 이 표를 쓴다).
# 두 시행 모두 각 4문항이라 순서만 정하면 된다 — NCS 표준 10영역 차례를 따랐다.
# 각 파일 머리말의 「05 매출액」 같은 번호는 **작성 순서**다. 최종 번호가 아니다 —
# `layout.py` 가 면을 채우려고 **같은 영역 안에서** 뒤쪽 단독 문항을 끌어올린다
# (영역을 넘나들지는 않는다). 실제로 6회에서 05·06이 뒤바뀌었다.
# 최종 번호는 빌드 로그의 `[정답순서]` 줄이 말한다.
AREAS = [
    ("s1_communication", "의사소통능력", 4),   # 01~04
    ("s2_math",          "수리능력",     4),   # 05~08
    ("s3_problem",       "문제해결능력", 4),   # 09~12
    ("s4_selfdev",       "자기개발능력", 4),   # 13~16
    ("s5_relation",      "대인관계능력", 4),   # 17~20
    ("s6_resource",      "자원관리능력", 4),   # 21~24
    ("s7_info",          "정보능력",     4),   # 25~28
    ("s8_tech",          "기술능력",     4),   # 29~32
    ("s9_org",           "조직이해능력", 4),   # 33~36
    ("s10_ethics",       "직업윤리",     4),   # 37~40
]

# 회차 프로파일 — 플레이북의 고정 수치 대신 이 값을 목표로 삼는다 (D48).
# 수치의 출처는 전부 시행 자료이거나 후기의 **소재·유형** 언급이다.
PROFILE = {
    "문항당 시간": "67.5초 — 45분 / 40문항. 건보(60초)보다 여유가 있다",
    "모듈 대 피셋": "모듈 14~16문항(35~40%) · 나머지 피셋",
    "모듈의 갈래": "산인공 모듈(표준 교재 항목) · 응용 모듈(공문서·엑셀·한글)",
    "세트문항 비율": "25~35% — 영역당 4문항뿐이라 세트를 크게 묶을 수 없다. "
                     "묶어도 한 영역 안에서 2문항까지다(SPEC 2절)",
    "지문 중앙값": "의사소통 600~800자 · 그 밖은 표·상황 위주로 짧게. "
                   "67.5초라 건보처럼 길게 쓰면 못 푼다",
    "각주(※) 개수": "회차당 3~5개 — 그 하나가 문제의 열쇠가 되게 한다",
    "<보기> 조합형": "2문항",
    "영역별 체감 난이도": "수리·문제해결 중상 · 모듈 6영역 중 · 의사소통 중",
    "소재": "도시철도 40% · 일반 사회 60%. 역·승강장·차량기지·환승·정기권처럼 "
            "**직무에서 만나는 장면**을 쓰되 특정 노선 실측치를 지어내지 않는다",
    "피셋 세부": "의사소통 내용일치·추리·연결어 / 수리 매출액·거리속력시간·"
                 "요금·시간차이 / 문제해결 조건부합 3·날짜유추 1 / "
                 "자원관리 자료해석 2 / 정보 출력코드 2·자료해석 1",
    "모듈 6영역의 형": "**전부 사례형.** 「다음 상황에서 …에 해당하는 것은?」 · "
                       "「두 사람을 …에 바르게 짝지은 것은?」. 개념 정의를 그대로 "
                       "묻지 않는다(SPEC 2절). 은행의 자기개발·대인관계 24문항이 그 본이다",
    "정답 배치": "40문항 기준 ①~⑤ 각 6~10개. 3연속 금지. "
                 "영역이 4문항뿐이라 한 영역에서 같은 번호를 3개 이상 쓰지 않는다",
}
