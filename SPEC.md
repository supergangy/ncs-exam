# NCS 봉투모의고사 제1회 — 문항 집필 스펙

## 1. 시험 개요

| 항목 | 내용 |
|---|---|
| 명칭 | 공기업 NCS 봉투모의고사 실전모의고사 1회 |
| 출제 유형 | **피듈형**(PSAT형 + 모듈형 혼합) |
| 문항 수 / 시간 | **50문항 / 60분** (문항당 평균 72초) |
| 선택지 | **5지선다 ① ② ③ ④ ⑤** |
| 기관 색채 | **범용 공기업** — 가상 기관 `○○공사`, `K공사`, `한국□□공단` 사용. 실존 기관명은 통계·제도 출처로만 언급 |
| 난이도 | **중상** — 실제 시험 체감 난이도. 정답률 55~70% 목표 |

## 2. 영역별 문항 배분 (문항 번호 고정)

| 순서 | 영역 | 문항 수 | 번호 | 담당 파일 |
|---|---|---|---|---|
| 1 | 의사소통능력 | 7 | 01~07 | `content/a1_communication.py` |
| 2 | 수리능력 | 7 | 08~14 | `content/a2_math.py` |
| 3 | 문제해결능력 | 7 | 15~21 | `content/a3_problem.py` |
| 4 | 자원관리능력 | 6 | 22~27 | `content/a4_resource.py` |
| 5 | 정보능력 | 6 | 28~33 | `content/a5_info.py` |
| 6 | 기술능력 | 6 | 34~39 | `content/a6_tech.py` |
| 7 | 조직이해능력 | 6 | 40~45 | `content/a7_org.py` |
| 8 | 직업윤리 | 5 | 46~50 | `content/a8_ethics.py` |

**번호는 스펙에 고정된 값이다.** 세트문항(`[08~09]`)을 만들 때 반드시 자기 영역 범위 안에서만 묶는다.

## 3. 데이터 스키마

각 담당 파일은 아래 형태의 `BLOCKS` 리스트 **하나만** 정의한다. 다른 코드·import 금지.

```python
# -*- coding: utf-8 -*-
BLOCKS = [
    {
        "area": "의사소통능력",
        "lead": None,                 # 세트문항이면 "[01~02] 다음 글을 읽고 물음에 답하시오."
        "passage": None,              # 공유 지문 HTML 문자열. 없으면 None
        "questions": [
            {
                "type": "내용추론",           # 문항 유형 라벨 (해설집에 표기)
                "stem": "다음 글을 읽고 추론할 수 있는 내용으로 가장 적절한 것은?",
                "material": None,             # 이 문항 전용 자료(표/보기 박스) HTML. 없으면 None
                "choices": [
                    "선택지 1 내용",
                    "선택지 2 내용",
                    "선택지 3 내용",
                    "선택지 4 내용",
                    "선택지 5 내용",
                ],
                "answer": 3,                  # 1~5 정수
                "explain": "<p>정답 근거를 서술한 HTML</p>",
                "each": [                     # 5개 원소. 각 선택지 단평. 정답 항목도 반드시 채운다.
                    "① ~는 2문단과 어긋난다.",
                    "② ~는 언급되지 않았다.",
                    "③ (정답) 3문단 마지막 문장의 내용과 일치한다.",
                    "④ ~는 반대로 서술되어 있다.",
                    "⑤ ~는 과도한 일반화다.",
                ],
            },
        ],
    },
]
```

### 필드 규칙
- `passage`가 있으면 그 블록의 모든 문항이 해당 지문을 공유하고, `lead`를 반드시 채운다.
- 단독 문항이면 `lead=None`, `passage=None`, 자료는 `material`에 넣는다.
- `choices`는 **정확히 5개**. 원문자 `①②③④⑤`를 문자열에 넣지 말 것 — 렌더러가 자동으로 붙인다.
- `answer`는 1~5 정수(원문자 아님).
- `each`는 **정확히 5개**, 각 문자열은 `① `, `② ` … 로 시작한다. 정답 항목에는 `(정답)`을 표기한다.

## 4. 허용 HTML

`passage` / `material` / `explain` 에서만 HTML을 쓴다. **아래 태그·클래스만** 허용한다.

| 용도 | 마크업 |
|---|---|
| 문단 | `<p>...</p>` |
| 강조 | `<strong>`, `<em>`, `<u>` |
| 줄바꿈 | `<br>` |
| 밑줄 지시어 | `<u class="ref">㉠</u>` |
| 조건/보기 박스 | `<div class="box"><div class="box-title">&lt;조건&gt;</div><p>ㄱ. …</p></div>` |
| 자료 표 | `<table class="data"><caption>&lt;표&gt; 제목</caption><tr><th>…</th></tr><tr><td>…</td></tr></table>` |
| 엑셀 시트 | `<table class="excel">` — 첫 행은 `<th>A</th><th>B</th>…`, 첫 열은 행번호 |
| 각주 | `<p class="note">※ 무역수지 = 수출액 − 수입액</p>` |
| 목록 | `<ul><li>`, `<ol><li>` |
| 단위 표기 | `<p class="unit">(단위: 억 원)</p>` — 표 바로 위 |
| 코드/수식 | `<code>=VLOOKUP(...)</code>` |
| 분수·첨자 | `<sup>1</sup>/<sub>12</sub>` — 해설의 분수 표기에만 사용 |
| 장문 지문 박스 | `<div class="passage">…</div>` — **단독 문항의 장문 지문**은 이 클래스로 감싸 세트 지문과 서식(테두리+명조)을 통일한다 |

### 4-1. 발문·리드문은 순수 텍스트

`stem` 과 `lead` 에는 **태그를 쓰지 않는다.** 강조하고 싶은 조건이 있으면 볼드를 넣지 말고 문장을 고쳐 쓴다.
`&lt;보기&gt;` 처럼 이스케이프한 문자열은 태그가 아니므로 괜찮다.

발문에 볼드를 넣으면 **어느 조건이 판정 지점인지 미리 알려주어 난이도가 떨어진다.**
`2024년에 대전지사에 설치된 세 번째 스토리지의 관리코드는?` 에서 조건부를 굵게 잡으면,
규칙표에서 무엇을 찾아야 하는지 응시자가 읽기 전에 이미 알게 된다.

### 4-2. 볼드·밑줄은 구조 표시에만

지문과 자료 안에서도 강조는 **문서의 뼈대를 보여줄 때만** 쓴다.

| | 쓴다 | 쓰지 않는다 |
|---|---|---|
| `<strong>` | 문서 제목(`정보시스템 장애 발생 보고서`), 조항 제목(`제1조(운임)`), 단계·항목 번호(`1단계 접수`, `2. 지원 대상`), 배열 기호(`(가)`) | **판정 근거가 되는 값·조건.** `8시간 이상`, `4시간까지`, `뒤 두 자리`, `E06` 같은 것 |
| `<u class="ref">` | 문항이 지시어로 가리키는 부분(`㉠`~`㉤`) | 규정문 안의 중요 문구 강조. `1박 80,000원을 초과할 수 없다` 같은 것 |

한도값·경계값·예외 조항을 굵게 잡으면 **오답 함정이 그대로 무력화된다.** 24~25번 출장여비의
숙박비 상한, 50번 봉사시간 한도가 실제로 그랬다. 눈에 띄어야 할 정보일수록 평문으로 둔다.

해설(`explain`·`each`)은 예외다. 계산 결과와 판정 지점을 짚어 주는 것이 해설의 일이므로 볼드를 쓴다.

- `<img>`, `<script>`, `style` 속성, 인라인 CSS **금지**. 그림이 필요한 문항은 만들지 않는다.
- 그래프가 필요하면 **표로 대체**한다.
- HTML 특수문자는 이스케이프: `<` → `&lt;`, `>` → `&gt;`, `&` → `&amp;`.
  - 예: 박스 제목은 `&lt;조건&gt;`, 부등호는 `&gt;=`.

## 5. 분량 기준 (참고 PDF 실측값)

| 구분 | 기준 |
|---|---|
| 의사소통 장문 지문 | **800~1,100자** (공백 포함). 4~5문단 |
| 의사소통 단문/실무문서 | 350~600자 |
| 정보·기술 설명 지문 | **900~1,300자** |
| 문제해결 규정·안내문 | 500~900자 (조·항 번호 구조) |
| 자원관리 자료 | 표 2~3개 + 각주 2~3줄 |
| 선택지 1개 | 15~70자. 5개 선택지 길이를 비슷하게 맞춘다 |
| 자료 표 | 행 4~8, 열 3~7 |

## 6. 영역별 유형 구성 지침

### 의사소통능력 (01~07)
- 장문 독해 3~4문항(일치/불일치, 추론, 문단 배열), 세트문항 1세트 포함
- **실무문서 기반 문항은 3개를 넘기지 않는다**(공문·회의록·이메일·보도자료·공고문 합산). 시판 봉투모의고사는 10문항당 1건 수준이며, 실무문서가 과하면 봉투모의고사가 아니라 실무 문제집이 된다
- **발문 중복 금지**: 같은 회차에 "㉠~㉤에 대한 검토 의견으로 적절하지 않은 것"류 발문을 두 번 쓰지 않는다. 발문은 일치·추론·문단배열·사례적용·중심내용으로 분산한다
- 소재: 최신 이슈 1개 이상(예: 생성형 AI 활용지침, 공공데이터 개방, 전력망 확충, 초고령사회, 탄소중립)

### 수리능력 (08~14)
- 응용수리 2문항(거리·속력·시간, 경우의 수·확률, 농도, 일률, 원가·할인 중 택2)
- 자료해석 5문항: 증감률, 비중, 순위 비교, 지수 계산, 표 2개 결합 해석
- **모든 수치는 실제로 계산이 맞아떨어져야 한다.** 반올림 조건은 문두에 명시
- 계산은 암산·간단한 필산으로 90초 내 가능해야 함

### 문제해결능력 (15~21)
- 명제·조건 추리 2문항(ㄱ~ㅂ 조건, 반드시 참인 것)
- 규정·약관 적용 2~3문항(요금 산정, 자격 판단)
- 순서·배치·일정 추론 1~2문항
- SWOT / 3C / 논리적 오류 등 모듈형 1문항

### 자원관리능력 (22~27)
- 시간관리 1~2(일정 수립, 최단 소요시간)
- 예산관리 2(견적 비교, 출장비·경비 계산)
- 물적자원 1(제품 선정 조건 비교표)
- 인적자원 1~2(인사평가 점수 산정, 팀 배치)
- 계산 결과가 선택지에 정확히 존재해야 함

### 정보능력 (28~33)
- 엑셀 함수 2~3문항(VLOOKUP, INDEX/MATCH, COUNTIF(S), SUMIF, IF 중첩, TEXT, CONCATENATE 등)
- 코드 부여 규칙 해석 세트 1세트(2문항)
- 최신 IT 지문 1~2문항: **생성형 AI, LLM, RAG, 온디바이스 AI, 제로트러스트, 랜섬웨어, 양자내성암호, 데이터센터 전력, 클라우드 네이티브** 중 선택
- 컴퓨터활용/정보처리 모듈 개념 1문항

### 기술능력 (34~39)
- 매뉴얼 이해 2문항(제품 사용설명서, 오류코드 표)
- 기술 지문 이해 2문항: **최신 기술 소재 필수** — 예) 히트펌프, ESS·BESS, SMR, 디지털 트윈, 수소환원제철, V2G, 전고체 배터리, HVDC
- 산업재해/기술선택/벤치마킹 모듈 1~2문항

### 조직이해능력 (40~45)
- 조직도·결재규정 해석 1~2문항
- 경영전략(SWOT, 마이클 포터, BCG) 1~2문항
- 국제 비즈니스 매너·환율/시차 1문항
- 조직문화·업무 프로세스 1~2문항

### 직업윤리 (46~50)
- 근로윤리(정직, 근면, 성실) 1~2문항
- 공동체윤리(봉사, 책임, 준법, 예절) 1~2문항
- 이해충돌방지법·청탁금지법 적용 사례 1문항
- 직장 내 괴롭힘/성희롱 대응 1문항
- 사례형으로 출제하고, 정답은 법령·지침에 근거해 명확히 갈리게 한다

## 7. 난이도 '중상' 기준

- 선택지 5개 중 **매력적인 오답 2개** 이상 배치. 지문에 있는 표현을 살짝 비틀어 만든다
- 자료해석은 **2단계 계산**(예: 증감률 구한 뒤 비교)을 기본으로 한다
- 조건추리는 경우의 수가 2~3갈래로 갈렸다가 하나로 수렴하게 설계
- 단순 암기 확인 문항은 영역당 1개 이하

## 8. 정답 분포

- 50문항 기준 **①~⑤ 각 8~12개**. 완전 균등(각 10개)에 억지로 맞추지 않는다
- 같은 번호가 **3연속** 정답이 되지 않게 한다
- 각 영역 안에서 특정 번호가 절반 이상을 차지하지 않게 한다

### 시판본 실측을 그대로 따르지 않는 이유

시판본 1,240문항에서는 ①이 균등 대비 0.80배로 일관되게 적고(8개 기관 중 7곳) 가운데 ③④가 많았다(`docs/ANSWER_DISTRIBUTION.md`).

**그러나 이 수치는 출제기관 기출이 아니라 출판사가 만든 모의고사에서 나온 것이다.** 실제 시험의 배치 관행이 아니라 집필자 습관일 가능성이 크다. 이를 재현하면 응시자에게 *"①은 답이 잘 아니다"* 라는 틀린 감각을 길들이게 되므로 **의도적인 ① 억제는 하지 않는다.**

균등에서 자연스럽게 흔들리는 정도(각 8~12개)만 허용하고, 위치 편향은 만들지 않는다. 실측값은 참고 자료로만 둔다.

## 9. 해설 작성 규칙

- `explain`: 정답 도출 과정을 3~8줄로. 계산 문항은 **식을 그대로 노출**한다
  - 예: `<p>휴게소까지 72.5km를 60km/h로 → 72.5 ÷ 60 = 1시간 12.5분</p>`
- 자료해석 해설에는 필요한 중간값 표를 `<table class="data">`로 붙여도 된다
- `each`: 오답이 왜 틀렸는지 **근거 위치**(몇 문단, 표의 어느 값)를 명시
- 모듈형은 관련 이론·법령명을 한 줄 덧붙인다

## 10. 저작권·표현 금지사항

- 지문은 **전부 새로 쓴다.** 기존 교재·기사·논문 문장을 그대로 옮기지 않는다
- 실제 통계를 소재로 쓰되 **수치는 문항용으로 재구성**한다
- **`※ 자료는 문항 구성을 위해 재구성한 것임` 같은 면책 문구는 넣지 않는다.** 모의고사 자체가 연습용이라는 것이 전제이고, 시판 봉투모의고사에도 이런 문구는 없다. 각주(`※`)는 계산식·적용 범위·예외 규정처럼 **문제 풀이에 필요한 정보에만** 쓴다
- 실존 인물, 특정 기업 비방, 정치적 편향, 성별·지역 고정관념 표현 금지
- 등장인물은 `김대리`, `이주임`, `박과장`, `A사원` 같은 익명 표기 사용

## 11. 산출물 검수 체크리스트 (집필자 자가검증)

작업을 끝내기 전에 스스로 확인한다.

1. `python -c "import ast,io; ast.parse(open('<파일>',encoding='utf-8').read())"` 로 문법 오류가 없는가
2. 문항 수가 배정된 개수와 정확히 같은가
3. 모든 `choices` 길이가 5, 모든 `each` 길이가 5인가
4. `answer`가 1~5 범위인가
5. 계산 문항의 답을 **직접 다시 계산**해서 선택지와 일치시켰는가
6. 지문에 정답 근거가 실제로 존재하는가, 복수정답 가능성은 없는가
7. HTML 특수문자를 이스케이프했는가

## 12. 지면 배치 — 펼침면 기준

실제 시험은 **두 면을 펼쳐 놓고** 푼다. 세트문항의 지문과 문항이 펼침을 넘어가면 페이지를 계속 넘겨야 하는데, 실전 시험에서 거의 볼 수 없는 형태다. `build.py`가 빌드마다 자동으로 배치를 잡는다(`layout.py`).

### 배치 규칙

1. **세트문항은 한 면에 통째로** 넣는다.
2. 한 면에 안 들어가는 세트는 **지문을 왼쪽(짝수) 면, 문항을 오른쪽(홀수) 면**에 둔다. 제본 시 마주보는 쌍은 (2,3)(4,5)… 이므로 짝수 면이 왼쪽이다.
3. 면 끝에 여백이 남으면 **같은 영역 안에서** 뒤쪽 단독 문항을 끌어올려 채운다.
4. **영역을 넘나드는 재배치는 절대 하지 않는다.** 영역 순서와 경계는 그대로 유지된다.
5. **문제집에는 영역 헤더를 넣지 않는다.** `의사소통능력` 같은 표제는 영역당 약 73px씩 도합 0.6면을 잡아먹는다. 영역 구성은 응시 안내 페이지의 구성표(영역별 문항 수와 번호 범위)로 충분하다. 한 면에 두 영역의 문항이 함께 놓이는 것은 원래부터 정상이며, 헤더가 없어져도 3·4번 규칙은 그대로 지켜진다. **해설집에는 채점·복습 편의를 위해 헤더를 남긴다.**

### 집필 시 유의점

- **문항 순서는 배치기가 바꿀 수 있다.** 같은 영역 안에서만 움직이므로 영역 배분은 그대로지만, 몇 번 문항이 될지는 빌드 후에 정해진다. 따라서 **문항끼리 "앞 문항에서 본 자료" 같은 상호 참조를 걸면 안 된다**(세트 내부의 `윗글`·`위 자료`는 세트가 통째로 움직이므로 안전하다).
- 세트 지문이 한 면(약 980px, A4 기준 대략 1,600자 + 표)을 넘으면 자동으로 펼침 좌우로 나뉜다. **지문을 그 이상 키우면 문항 쪽 면이 비게 되므로** 분량을 조절한다.
- 배치 결과는 빌드 로그의 `[배치]` 섹션에 면별 채움률로 표시된다. **채움 55% 미만 면이 여럿이면 분량 설계를 손봐야 한다는 신호다.**

### 구현 메모

- Chrome은 CSS `break-before: left|right` 를 **무시한다**(실측 확인: 일반 페이지 넘김으로 처리하고 빈 면도 넣지 않는다). 그래서 면 번호를 파이썬에서 직접 계산해 `break-before: page` 를 심는다.
- 블록 높이는 헤드리스 Chrome `--dump-dom` 으로 실측한다. `getBoundingClientRect` 는 마진을 빼고 재므로, 블록 자체 높이와 블록 간 간격을 분리해 계산한다. 꼬리 마진은 면 끝에서 자리를 차지하지 않으므로 용량에 넣지 않는다.
- 면 용량은 실측 보정값이다(`layout.py`의 `SAFETY`). 조판 CSS를 바꾸면 재보정이 필요하다.
- `--no-layout` 으로 끄면 작성 순서 그대로 출력된다.
- `--html` 은 실측·재배치까지 그대로 하고 **PDF 변환과 쪽번호 스탬프만 건너뛴다.** 문구를 고쳐 가며 확인할 때 쓴다. `out/exam.html` 을 브라우저로 열고, 쪽나눔은 Ctrl+P(A4·배율 100%·여백 없음)로 본다.
